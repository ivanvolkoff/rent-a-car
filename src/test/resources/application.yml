spring:
  datasource:
    url: jdbc:h2://mem:hotel_db;DB_CONSE_DELAY=-1
    username: sa
    password: sa
    driver-class-name: com.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true